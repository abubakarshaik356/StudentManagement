🎓 Student Management System – Java Servlets & MySQL

Description:
The Student Management System is a web-based CRUD application developed using Java Servlets, JSP, JDBC, and MySQL. It allows administrators to add, view, update, and delete student records through a user-friendly interface. The project follows the MVC architecture, ensuring a clean separation between business logic, database operations, and presentation.

✨ Key Features:

Add, edit, view, and delete student details (CRUD operations)

Implemented Servlets (Controller), JSP (View), and JDBC (Model) under MVC pattern

MySQL database integration for persistent data storage

Server-side validation and exception handling for reliability

Responsive UI using HTML5, CSS3, and JSP

Deployed on Apache Tomcat Server

🛠️ Tech Stack:

Frontend: HTML5, CSS3, JSP

Backend: Java Servlets, JDBC

Database: MySQL

Server: Apache Tomcat

Architecture: MVC (Model–View–Controller)

🚀 How to Run:

Clone the repository.

Import the project into Eclipse IDE or IntelliJ IDEA as a Dynamic Web Project.

Create the MySQL database using the provided SQL script (students.sql).

Update database credentials in DBConnection.java.

Deploy the project on Apache Tomcat Server.

Access the app at http://localhost:8080/StudentManagementSystem/.
